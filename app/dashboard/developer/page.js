import { PageHeader, PageTransition } from '@/components/amarkt/kit'; import { Card } from '@/components/ui/card'
export default function DeveloperPage(){return <PageTransition className="space-y-6"><PageHeader title="Developer / API" subtitle="Connect a thin app through the stable API, SDK, streams, jobs, webhooks and artifacts."/><Card className="border-white/[.07] bg-white/[.02] p-5"><h2 className="font-semibold">TypeScript SDK</h2><pre className="mt-3 overflow-x-auto rounded bg-black/30 p-4 text-xs">{`const client = new AmarktAIClient({ apiKey })\nconst job = await client.execute({\n  capability: "video_generation",\n  input: { prompt: "...", durationSeconds: 8, aspectRatio: "16:9" }\n})`}</pre><p className="mt-3 text-xs text-muted-foreground">An exact route is accepted only when that app capability uses the app-selectable allowlist and the route was approved during onboarding.</p></Card></PageTransition>}
